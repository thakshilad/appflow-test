import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'appflow-test',
  webDir: 'dist/appflow-test',
  bundledWebRuntime: false
};

export default config;
